{:paths ["src" "test"]
 :tasks
 {test {:doc "Run ossekai murakumo actor boundary tests"
        :task (do
                (require 'clojure.test
                         'ossekai.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'ossekai.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
